import * as React from 'react';
import * as ReactDOM from 'react-dom';
import FinalActionButton from '../.';

const App = () => {
  return <FinalActionButton />;
};

ReactDOM.render(<App />, document.getElementById('root'));
